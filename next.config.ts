import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['cdn.britannica.com'], // Add the external image domain here
  },
};

export default nextConfig;
